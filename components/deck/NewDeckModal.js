import React from 'react';
import PropTypes from 'prop-types';

export default class NewDeckModal extends React.Component {
  static propTypes = {
    navigator: PropTypes.object,
  };

  constructor(props) {
    super(props);
  }

  render() {
    return (
      
    );
  }
}
