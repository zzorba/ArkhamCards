import { createIconSetFromIcoMoon } from 'react-native-vector-icons';

import circleIconConfig from './circle.json';

/* eslint-disable no-undef */
export default CircleIcon = createIconSetFromIcoMoon(circleIconConfig);
