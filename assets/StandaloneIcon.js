import { createIconSetFromIcoMoon } from 'react-native-vector-icons';

import standaloneIconConfig from './standalone.json';

/* eslint-disable no-undef */
export default StandaloneIcon = createIconSetFromIcoMoon(standaloneIconConfig);
