import { createIconSetFromIcoMoon } from 'react-native-vector-icons';

import coreSetIconConfig from './core.json';

/* eslint-disable no-undef */
export default CoreSetIcon = createIconSetFromIcoMoon(coreSetIconConfig);
